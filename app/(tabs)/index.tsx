/**
 * app/(tabs)/index.tsx
 *
 * @purpose Đầu nối (Entry point) từ Tab Router vào lõi Screen.
 * @why Router chỉ là Bảng Chỉ Đường, toàn bộ UI nằm tại screens/HomeScreen.tsx.
 */
import HomeScreen from '@/screens/HomeScreen';

export default HomeScreen;
