/**
 * app/movie/[id].tsx
 *
 * @purpose Đầu nối (Entry point) từ Router vào lõi Screen.
 * @why Router chỉ là Bảng Chỉ Đường, toàn bộ UI nằm tại screens/MovieDetailScreen.tsx.
 */
import MovieDetailScreen from '@/screens/MovieDetailScreen';

export default MovieDetailScreen;
